#pragma once

#include <stdafx.hpp>

#include <nlohmann/json.hpp>

namespace fox::json
{
	static void test()
	{
		//auto config = std::ifstream{ "C:/Users/jariw/source/repos/Fox/Fox/src/Fox/Config/config.json" };
		//auto res    = config.is_open();

		//auto json = nlohmann::json::parse(config);
		//std::string result{};
		//auto& a = json.at("engine").at("name").get_to(result);
		//__debugbreak();
	}
}
